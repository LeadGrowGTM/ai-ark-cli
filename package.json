{
  "name": "ai-ark-cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "ai-ark": "./dist/index.js"
  },
  "scripts": {
    "setup": "bun run scripts/setup.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.0"
  }
}
